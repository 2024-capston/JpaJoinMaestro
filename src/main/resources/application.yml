spring:
  application:
    name: jpaJoinMaestro

  datasource:
    url: jdbc:mysql://localhost:3306/order_test
    username: root
    password: tjdvkrTm150*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    show-sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE