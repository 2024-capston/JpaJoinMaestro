spring:
  application:
    name: jpaJoinMaestro

  datasource:
    url: jdbc:mysql://localhost:3306/jpa
    username: root
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true